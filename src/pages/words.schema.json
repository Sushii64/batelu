{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "word": {
        "type": "string",
        "description": "The word in Batelu."
      },
      "definition": {
        "type": "string",
        "description": "The word's meaning in English."
      },
      "etymology": {
        "oneOf": [
          { "enum": [false] },
          {
            "type": "array",
            "prefixItems": [
              { "type": "string" },
              {
                "oneOf": [
                  { "type": "string" },
                  {
                    "type": "array",
                    "prefixItems": [{ "type": "string" }, { "type": "string" }]
                  }
                ]
              },
              { "type": "string" }
            ]
          }
        ],
        "description": "The word's etymology.\nIn the format of [\"Language\", [\"word\", \"transcription\"], \"meaning\"].\nIf the language already uses the Latin script, the format is [\"Language\", \"word\", \"meaning\"].\nIf the word is a priori, use false.\nIf the source language is Batelu or English, don't put a meaning."
      },
      "type": {
        "enum": [
          "noun",
          "verb",
          "particle",
          "interjection",
          "modifier",
          "numeral",
          "adposition",
          "conjunction",
          "pronoun"
        ],
        "description": "The part of speech of the word."
      },
      "date": {
        "type": "string",
        "pattern": "^\\d\\d-\\d\\d-\\d\\d\\d\\d$",
        "description": "The date the word's poll closed, after all tiebreakers.\nIn DD-MM-YYYY format."
      },
      "usage": {
        "type": "string",
        "description": "Usage information."
      }
    },
    "required": ["word", "definition", "etymology", "type", "date"],
    "additionalProperties": false
  }
}
